version: "3.8"

services:
  knowledge:
    build:
      context: .
      dockerfile: dockerfile
    container_name: knowledge
    ports:
      - "8050:80"
    networks:
      - backend
    volumes:
      - ./${CHROMA_PERSIST_DIRECTORY}:/var/www/html/knowledge/${CHROMA_PERSIST_DIRECTORY}
    command: /bin/sh -c "uvicorn src.main:app --host=0.0.0.0 --port=80"

volumes:
  chroma_db:

networks:
  backend:
    driver: bridge
