# راهنمای رفع مشکل URL در پایگاه دانش

در برخی موارد، URL اضافه شده به پایگاه دانش ممکن است در لیست منابع داده نمایش داده نشود. این راهنما برای کمک به شناسایی و رفع این مشکل ایجاد شده است.

## تغییرات اعمال شده

برای رفع مشکل عدم نمایش URL در لیست منابع داده، تغییرات زیر اعمال شده است:

1. اصلاح کلاس `RAGSystem.update_knowledge_base` برای حفظ دقیق URL در متادیتا
2. اصلاح کلاس `TextProcessor.process_document` برای اطمینان از حفظ URL دقیق در پردازش متن
3. اصلاح اندپوینت `/add_knowledge` برای استفاده مستقیم از خزنده و پردازش داده‌ها
4. ایجاد اسکریپت تشخیصی برای بررسی وجود URL در پایگاه دانش

## آزمایش رفع مشکل

برای آزمایش رفع مشکل، می‌توانید از اسکریپت‌های تشخیصی ایجاد شده استفاده کنید:

### 1. بررسی وجود URL در پایگاه دانش

```bash
python scripts/check_url_in_db.py "https://www.satia.co/%D8%A8%DB%8C%D8%A7%D9%86%DB%8C%D9%87-%D9%85%D8%A7%D9%85%D9%88%D8%B1%DB%8C%D8%AA//"
```

این اسکریپت بررسی می‌کند که آیا URL مورد نظر در پایگاه دانش وجود دارد یا خیر. همچنین URL‌های مشابه را نیز پیدا می‌کند.

### 2. آزمایش افزودن URL جدید و بررسی آن

```bash
python scripts/test_url_knowledge_fix.py "https://www.satia.co/blog"
```

این اسکریپت یک URL جدید را به پایگاه دانش اضافه می‌کند و سپس بررسی می‌کند که آیا در لیست منابع داده نمایش داده می‌شود یا خیر.

## روش‌های دیگر رفع مشکل

اگر همچنان مشکل وجود دارد، روش‌های زیر را امتحان کنید:

1. **حذف و اضافه مجدد**:
   ```bash
   # استفاده از اندپوینت update_knowledge برای خزش مجدد
   curl -X 'POST' \
     'http://localhost:8001/update_knowledge' \
     -H 'accept: application/json' \
     -H 'Content-Type: application/json' \
     -d '{
     "url": "https://www.satia.co/%D8%A8%DB%8C%D8%A7%D9%86%DB%8C%D9%87-%D9%85%D8%A7%D9%85%D9%88%D8%B1%DB%8C%D8%AA//"
   }'
   ```

2. **استفاده از API جدید برای افزودن متن ساده**:
   ```bash
   curl -X 'POST' \
     'http://localhost:8001/api/add_plaintext' \
     -H 'accept: application/json' \
     -H 'Content-Type: application/json' \
     -d '{
     "text": "متن مورد نظر",
     "title": "عنوان",
     "source": "https://www.satia.co/k-wireless/"
   }'
   ```

## نکات مهم

1. مطمئن شوید که URL دقیقاً به همان شکلی که در درخواست وارد می‌کنید، در متادیتا ذخیره می‌شود.
2. URL‌ها به فرمت حساس هستند، بنابراین ممکن است یک URL با فرمت‌های مختلف (مثلاً با یا بدون `/` انتهایی) به عنوان URL‌های متفاوت در نظر گرفته شوند.
3. در صورت ادامه مشکل، بررسی کنید که داده‌ها در ChromaDB به درستی ذخیره شده‌اند. 